<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="platform.education.generalTeachingAffair.dao.BasicSQLDao">

    <update id="update">
        ${args[0].sql}
    </update>

    <select id="find" resultType="map">
        ${args[0].sql}
    </select>

    <select id="findByPaging" resultType="map">
         ${args[0]}
    </select>
    <insert id="create">
         ${args[0]}
    </insert>

</mapper>